# Backend Environment Variables - Example
# Copy to .env and fill values

# Core
NODE_ENV=development
PORT=5001
MONGODB_URI=mongodb://127.0.0.1:27017/rgo-organic-millets

# Auth
JWT_SECRET=replace_with_strong_random_secret
JWT_EXPIRES_IN=7d

# Frontend
FRONTEND_URL=http://localhost:5173

# Google Auth (optional)
GOOGLE_CLIENT_ID=

# Admin Setup (for POST /api/auth/create-admin)
ADMIN_SETUP_KEY=shashank-reddy

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Stripe (optional)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
